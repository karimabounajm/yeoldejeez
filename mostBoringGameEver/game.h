#ifndef GAME_H
#define GAME_H

/**
	okayo, now is the time to write some actually cool code; I was thinking about writing in an algorithm
	for the enemies to chase the player using manhattan distance. it would also be cool to have the entire
	program run in real time, meaning that instances pass even if we the user doesn't enter a move, so it 
	would be like a real game
 */

int playGame(const char* filename);

#endif // GAME_H
